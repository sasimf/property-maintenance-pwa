import React,{useState,useContext}from'react';import{login}from'../../services/api';import{AuthContext}from'../../context/AuthContext';function Login(){const[e,setE]=useState('');const[p,setP]=useState('');const{setUser}=useContext(AuthContext);const s=async ev=>{ev.preventDefault();const u=await login({email:e,password:p});setUser(u);};return(<form onSubmit={s}><h2>Login</h2><input type="email" value={e} onChange={ev=>setE(ev.target.value)} placeholder="Email"/><input type="password" value={p} onChange={ev=>setP(ev.target.value)} placeholder="Password"/><button type="submit">Login</button></form>);}export default Login;